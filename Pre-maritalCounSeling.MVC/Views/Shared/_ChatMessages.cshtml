@{
    var messages = ViewData["ChatMessages"] as List<string>;
}

@if (messages != null && messages.Any())
{
    foreach (var message in messages)
    {
        <div class="chat-message">@message</div>
    }
}
else
{
    <div class="chat-message">No messages yet.</div>
}
